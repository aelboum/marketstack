"""Mini bookkeeping module: chart of accounts, journal entries, ledger,
periods, a tenant's own customer invoices/payments, expenses, bank
transaction import/reconciliation. Deliberately small scope -- see
docs/ACCOUNTING-SCOPE.md for what is explicitly out of scope. Never
shares a table, model class, or service module with core.billing."""
