"""Snapshot/template export-import: cloning configuration (workflows,
pipelines, forms, campaigns, calendars, landing pages, email/SMS
templates, settings) across tenants. Product-specific; the cloning
mechanism reuses the installed saas-os infra.db's ordinary tenant-scoped
write path."""
