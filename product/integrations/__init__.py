"""External-provider adapters: SMS, WhatsApp, telephony, calendar sync,
review platforms, bank-data aggregation, and any other Category D
dependency (docs/INTEGRATIONS.md). Every adapter here implements a
Protocol this product owns, following the same Protocol+Fake+adapter
pattern saas-os uses internally for Stripe/SMTP."""
