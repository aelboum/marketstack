# Backend API base URL. NEXT_PUBLIC_* is inlined into the client bundle at
# build time (Next.js convention) -- never put a secret in a NEXT_PUBLIC_*
# variable.
NEXT_PUBLIC_API_URL=http://localhost:8000
