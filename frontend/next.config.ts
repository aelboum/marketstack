import type { NextConfig } from "next";

// Minimal foundation config (docs/ROADMAP.md Phase 1.6) -- no
// product/dashboard configuration yet.
const nextConfig: NextConfig = {};

export default nextConfig;
