import { DomainPlaceholder } from "@/components/shell/DomainPlaceholder";

export default function AppointmentsPlaceholderPage() {
  return (
    <DomainPlaceholder
      title="Appointments"
      description="Calendars, availability, and booking flows."
      uiPhase="UI-6"
    />
  );
}
