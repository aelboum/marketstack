import { DomainPlaceholder } from "@/components/shell/DomainPlaceholder";

export default function ConversationsPlaceholderPage() {
  return (
    <DomainPlaceholder
      title="Conversations"
      description="Unified inbox across email, SMS, and WhatsApp threads."
      uiPhase="UI-4"
    />
  );
}
