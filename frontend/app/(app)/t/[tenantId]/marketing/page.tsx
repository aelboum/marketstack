import { DomainPlaceholder } from "@/components/shell/DomainPlaceholder";

export default function MarketingPlaceholderPage() {
  return (
    <DomainPlaceholder
      title="Marketing"
      description="Campaigns, forms, suppressions, and templates."
      uiPhase="UI-5"
    />
  );
}
