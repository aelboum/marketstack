import { DomainPlaceholder } from "@/components/shell/DomainPlaceholder";

export default function CrmPlaceholderPage() {
  return (
    <DomainPlaceholder
      title="CRM"
      description="Contacts, companies, opportunities, and pipelines."
      uiPhase="UI-3"
    />
  );
}
